# This is an example neoforge.mods.toml file. It contains the data relating to the loading mods.
# There are several mandatory fields (#mandatory), and many more that are optional (#optional).
# The overall format is standard TOML format, v0.5.0.
# Note that there are a couple of TOML lists in this file.
# Find more information on toml format here:  https://github.com/toml-lang/toml

# The license for you mod. This is mandatory metadata and allows for easier comprehension of your redistributive properties.
# Review your options at https://choosealicense.com/. All rights reserved is the default copyright stance, and is thus the default here.
license="${mod_license}"

# A URL to refer people to when problems occur with this mod
#issueTrackerURL="https://change.me.to.your.issue.tracker.example.invalid/" #optional

# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]] #mandatory

# The modid of the mod
modId="${mod_id}" #mandatory

# The version number of the mod
version="${mod_version}" #mandatory

# A display name for the mod
displayName="${mod_name}" #mandatory

# A URL to query for updates for this mod. See the JSON update specification https://docs.neoforged.net/docs/misc/updatechecker/
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional

# A URL for the "homepage" for this mod, displayed in the mod UI
#displayURL="https://change.me.to.your.mods.homepage.example.invalid/" #optional

# A file name (in the root of the mod JAR) containing a logo for display
#logoFile="examplemod.png" #optional

# A text field displayed in the mod UI
#credits="" #optional

# The authors of the mod, displayed in the mod UI (optional)
#authors=""

# The description text for the mod (multi line!) (#mandatory)
description='''
QOLVaultsAndNotes
Items in the game:
Bulletin board
Safe
Lock
Key
Keycard

Recipes for the items are unlocked through regular gameplay.

Bulletin Board:
A wall-mounted block that lets you write and store up to 8 sticky notes. Right click the block to open the board. Click the side panel toggle to open the note editor, where you can:
Write a title (up to 12 characters) and a multiline body
Choose from 4 note colors: yellow, green, blue and pink
Pin the note to the board using 2 paper (new notes) or 1 ink sac (edits)
Click any pinned note on the board to view or edit it
Paper and ink sacs are loaded into the bulletin board by right clicking it while holding them.

Safe:
A 2-row (18 slot) lockable storage block with an animated lid. To secure it:
Right click with a Lock to lock the safe. Only accessible by the player who put a lock on the safe/vault.
Right click with a named Key (dot/dash format, up to 18 characters) to assign a serial code, opening a visual segment-based entry screen when accessed
Or right click with a named Keycard to assign a numeric PIN, opening a number pad entry screen when accessed
Both the key and keycard are consumed on use. Without the correct code entered, the safe cannot be opened.
Takes quite a while to break.

Vault:
A large 72-slot (8 row × 9 column) storage block with a scrollable GUI. Supports the same lock, key, and keycard security system as the safe.
Created by placing safe block in a 2x2x2 formation.
Takes a LONG time to break.

Keycard Extra Interactions:
Nether Portal - right click the obsidian frame of a nether portal to toggle it on/off.
Piston - right click a piston from the side to force extend or retract it, overriding redstone signals and holding it in place until clicked again

Key Extra Interactions:
Change=block state of an iron door.
Untrap a trapped chest.
  '''

# The [[mixins]] block allows you to declare your mixin config to FML so that it gets loaded.
#[[mixins]]
#config="${mod_id}.mixins.json"

# The [[accessTransformers]] block allows you to declare where your AT file is.
# If this block is omitted, a fallback attempt will be made to load an AT from META-INF/accesstransformer.cfg
#[[accessTransformers]]
#file="META-INF/accesstransformer.cfg"

# The coremods config file path is not configurable and is always loaded from META-INF/coremods.json

# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.${mod_id}]] #optional
    # the modid of the dependency
    modId="neoforge" #mandatory
    # The type of the dependency. Can be one of "required", "optional", "incompatible" or "discouraged" (case insensitive).
    # 'required' requires the mod to exist, 'optional' does not
    # 'incompatible' will prevent the game from loading when the mod exists, and 'discouraged' will show a warning
    type="required" #mandatory
    # Optional field describing why the dependency is required or why it is incompatible
    # reason="..."
    # The version range of the dependency
    versionRange="[${neo_version},)" #mandatory
    # An ordering relationship for the dependency.
    # BEFORE - This mod is loaded BEFORE the dependency
    # AFTER - This mod is loaded AFTER the dependency
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT, or SERVER
    side="BOTH"

# Here's another dependency
[[dependencies.${mod_id}]]
    modId="minecraft"
    type="required"
    # This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

# Features are specific properties of the game environment, that you may want to declare you require. This example declares
# that your mod requires GL version 3.2 or higher. Other features will be added. They are side aware so declaring this won't
# stop your mod loading on the server for example.
#[features.${mod_id}]
#openGLVersion="[3.2,)"
